<UserControl x:Class="ModuleLogsProvider.GUI.Views.ErrorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300"
			 Visibility="{Binding Visibility, Mode=OneWay}" >
	<Grid Background="#3AFF0000">
		<Rectangle Stroke="Red"/>
		<StackPanel Orientation="Horizontal" Margin="5">
			<TextBlock Text="{Binding LastErrorTime, Mode=OneWay, StringFormat=HH:mm:ss}" Margin="0,0,5,0"/>
			<TextBlock Text="{Binding ErrorMessage, Mode=OneWay}"/>
		</StackPanel>
	</Grid>
</UserControl>
