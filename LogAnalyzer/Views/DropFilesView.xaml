<UserControl x:Class="LogAnalyzer.GUI.Views.DropFilesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:Common="clr-namespace:LogAnalyzer.GUI.Common"
             xmlns:ViewModels="clr-namespace:LogAnalyzer.GUI.ViewModels" mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300"
			 Background="Transparent"
			 AllowDrop="True">

	<Common:AttachedCommandBehaviour.Bindings>
		<Common:AttachedCommandBinding Command="{Binding DropCommand}" Event="Drop" />
	</Common:AttachedCommandBehaviour.Bindings>

	<UserControl.Resources>

		<BooleanToVisibilityConverter x:Key="bool2VisibilityConverter"/>

		<DataTemplate DataType="{x:Type ViewModels:DroppedFileViewModel}" x:Key="droppedFileTemplate">
			<StackPanel Orientation="Vertical">
				<StackPanel Orientation="Horizontal"
							Margin="3">
					<Button ToolTip="Remove this file"
							Margin="3,0,3,0"
							Command="{Binding RemoveFileCommand}">
						<Image Source="pack://application:,,,/LogAnalyzer.GUI;component/Resources/cross-small-black.png"/>
					</Button>
					<TextBlock Text="{Binding FileName}"/>
				</StackPanel>
			</StackPanel>
		</DataTemplate>

	</UserControl.Resources>

	<Grid>
		<TextBlock HorizontalAlignment="Center"
				   VerticalAlignment="Center"
				   FontSize="20"
				   Visibility="{Binding HasNoFiles, Converter={StaticResource bool2VisibilityConverter}}">
			Drop files here to analyze them
		</TextBlock>

		<ItemsControl ItemsSource="{Binding Files}"
					  Visibility="{Binding HasFiles, Converter={StaticResource bool2VisibilityConverter}}"
					  HorizontalAlignment="Center"
					  VerticalAlignment="Center"
					  ItemTemplate="{StaticResource droppedFileTemplate}"
					  />

	</Grid>
</UserControl>
