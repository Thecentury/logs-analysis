<UserControl x:Class="LogAnalyzer.GUI.View.FilterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             
 			 xmlns:core="clr-namespace:LogAnalyzer;assembly=LogAnalyzer"
			 xmlns:vm="clr-namespace:LogAnalyzer.GUI.ViewModel"
			 xmlns:view="clr-namespace:LogAnalyzer.GUI.View"
			 xmlns:local="clr-namespace:LogAnalyzer.GUI"

			 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
	
	<DockPanel LastChildFill="True">
		<ToolBar DockPanel.Dock="Top" Margin="4,4,4,4">
			<Button Style="{StaticResource toolbarButtonStyle}"
					Command="{Binding EditFilterCommand}" 
					ToolTip="Edit current filter">
				<Image Source="..\Resources\pencil.png" Stretch="None"/>
			</Button>
			<Button Style="{StaticResource toolbarButtonStyle}"
					Command="{Binding RefreshCommand}" 
					ToolTip="Refresh view">
				<Image Source="..\Resources\arrow-circle-double.png" Stretch="None"/>
			</Button>
		</ToolBar>
		
		<view:LogEntriesListView/>
	</DockPanel>
</UserControl>
